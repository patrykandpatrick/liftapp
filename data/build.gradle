apply plugin: "com.android.library"
apply from: "$rootDir/gradle/base.gradle"

dependencies {

    dataModuleDependencies.each {

        add(it.configuration, it.dependency, it.options)
    }
}

android {

    namespace "com.patrykandpatryk.liftapp.data"
}
