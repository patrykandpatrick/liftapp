apply from: "$rootDir/gradle/functionality-module-base.gradle"

dependencies {

    storageModuleDependencies.each {

        add(it.configuration, it.dependency, it.options)
    }
}

android {

    namespace "com.patrykandpatryk.liftapp.functionality.storage"
}
