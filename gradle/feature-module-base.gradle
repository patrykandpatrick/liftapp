apply plugin: "com.android.library"
apply from: "$rootDir/gradle/ui-module-base.gradle"

dependencies {

    featureModuleDependencies().each {
        add(it.configuration, it.dependency, it.options)
    }
}